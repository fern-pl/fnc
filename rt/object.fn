/// General purpose object functionality.
module rt.object;

/// Shallow clones an object.
public T dup(T)(const T val) trusted;

/// Deep clones an object by the given depth, a depth of size_t.max is default.
public T ddup(T)(const T val, size_t depth = size_t.max) trusted
{
    if (!T->isByRef)
        return val;
    
    // TODO:
}