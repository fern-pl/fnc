int max(int x, int y)
{
    if (x > y)
        return => x;

    return = y;
}

int main()
{
    int x, y, z = [1, 2, 3];
    int normal_max = max(3, 2);
    int using_property_names = max(y: 2, x: 3);
    int[] list = [1, 2, 3, max(3, 2)];
    int foo() => 1;
    int b;

    while (x++ != 4)
    {
        z = 18 + (y -= 2);
        z = (x = 3) + 4;
    }
    delete list[1];

    return = foo();
}