module a;

@xmmword struct A 
{
    uint a;
    ushort b; 

    // This is a comment. // Still a comment. \\ Still still a comment.
    uint foo()
    {
       return = a;  
    }
}

struct B
{
    uint a;
    byte b;

    byte bar()
    {
       return = b;
    }
}

void main()
{
    A a;
    uint b = a.foo();
    byte c = a |> B.bar();
}